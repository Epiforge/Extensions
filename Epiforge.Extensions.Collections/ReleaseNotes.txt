A dictionary raising its change notification to a subscriber which takes keys and values as objects allocates substantially less. The projection which boxes each key and value no longer builds a lazy sequence the receiving event arguments then have to enumerate at an unknown length; each list is now sized in advance from the count already known. This applies to every observable dictionary, and a single-key replacement heard by such a subscriber allocates around a fifth less.
